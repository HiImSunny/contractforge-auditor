# ContractForge Auditor — Frontend environment variables
# Copy to .env.local and fill in your values before running.

# Required: Base URL of the deployed backend API (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000
